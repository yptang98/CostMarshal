interface:
  display_name: "CostMarshal"
  short_description: "Route APIs with bounded native Codex teams."
  default_prompt: "Use $costmarshal to route this task across reviewed APIs and bounded native Codex child agents with budget controls."

policy:
  allow_implicit_invocation: false
