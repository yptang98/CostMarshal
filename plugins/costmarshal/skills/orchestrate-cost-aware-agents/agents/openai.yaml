interface:
  display_name: "CostMarshal Orchestrator"
  short_description: "Route APIs with bounded native Codex teams"
  default_prompt: "Use $orchestrate-cost-aware-agents to complete this task with cost-aware API routing and bounded native Codex child agents."

policy:
  allow_implicit_invocation: true
